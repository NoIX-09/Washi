---
import Layout from '../../layouts/Layout.astro';
import { Icon } from 'astro-icon/components';
import { getCollection } from 'astro:content';
import { t, LOCALES, type Locale } from '../../i18n/translations';

export function getStaticPaths() {
  return LOCALES.map(l => ({ params: { locale: l } }));
}

const locale = (Astro.currentLocale || 'zh-CN') as Locale;
const posts = (await getCollection('blog')).sort((a, b) => b.data.date.localeCompare(a.data.date));
---

<Layout title="Blog">
  <div class="page">
    <h1 class="page-title">{t('blog.title', locale)}</h1>
    <hr class="pencil-line wide" />
    <div class="blog-list">
      {posts.length > 0 ? (
        posts.map((post) => (
          <a class="blog-card" href={`/${locale}/blog/${post.id}`}>
            <div class="blog-icon">
              <Icon name="ph:notebook-duotone" size="24" />
            </div>
            <div class="blog-info">
              <h3>{post.data.title}</h3>
              <p>{post.data.desc}</p>
              <time>{post.data.date}</time>
            </div>
          </a>
        ))
      ) : (
        <div class="empty-hint">
          <Icon name="ph:notebook-duotone" size="40" />
          <p>{t('blog.emptyHint', locale)}</p>
        </div>
      )}
    </div>
  </div>
</Layout>

<style>
  .page {
    flex: 1;
    width: 100%;
    max-width: 880px;
    padding: 40px 24px 0;
    box-sizing: border-box;
  }
  .page-title {
    font-family: 'LXGW WenKai', 'Kaiti SC', 'KaiTi', 'STKaiti', serif;
    font-size: 28px;
    font-weight: 500;
    color: var(--c-heading);
    margin: 0 0 28px;
  }
  .blog-list { display: flex; flex-direction: column; gap: 14px; }
  .blog-card {
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 24px;
    border-radius: 12px;
    background: var(--c-surface);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    text-decoration: none;
    transition: transform .2s, background .2s;
  }
  .blog-card:hover { transform: translateY(-2px); background: var(--c-surface-strong); }
  .blog-icon {
    width: 48px;
    height: 48px;
    border-radius: 10px;
    background: rgba(var(--c-accent-rgb), .1);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .blog-icon :global(svg) { color: var(--c-accent); }
  .blog-info { min-width: 0; }
  .blog-info h3 {
    margin: 0;
    font-family: 'LXGW WenKai', 'Kaiti SC', 'KaiTi', 'STKaiti', serif;
    font-size: 16px;
    font-weight: 500;
    color: var(--c-heading);
  }
  .blog-info p { margin: 6px 0 0; font-size: 13px; color: var(--c-text); line-height: 1.6; }
  .blog-info time { display: block; margin-top: 8px; font-family: 'LXGW WenKai Mono', 'LXGW WenKai', 'Kaiti SC', 'KaiTi', 'STKaiti', monospace; font-size: 12px; color: var(--c-muted-dim); }
  .empty-hint {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    padding: 60px 0;
    color: var(--c-muted-dim);
  }
  .empty-hint p { margin: 0; font-family: 'LXGW WenKai', 'Kaiti SC', 'KaiTi', 'STKaiti', serif; font-size: 15px; }

  @media (max-width: 600px) {
    .page { padding: 32px 16px 0; }
    .page-title { font-size: 22px; margin: 0 0 20px; }
    .blog-card { padding: 16px; gap: 14px; }
    .blog-icon { width: 40px; height: 40px; border-radius: 8px; }
    .blog-icon :global(svg) { width: 20px; height: 20px; }
    .blog-info h3 { font-size: 15px; }
  }
</style>
